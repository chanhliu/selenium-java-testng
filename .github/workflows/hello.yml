name: Hello Actions

on:
  push:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello from GitHub Actions ${{JENKINS_URL}}"
      - name: Ping Jenkins
        run: curl https://subjective-font-search-frame.trycloudflare.com
